# ============================================
# Cloudflare Workers 配置文件
# 項目: Flux AI Pro - NanoBanana Edition
# 版本: 11.15.0
# 更新: 2026-02-18
# ============================================

name = "flux-ai-pro"
main = "worker.js"
compatibility_date = "2024-12-01"

# ============================================
# KV 存儲綁定 (NanoBanana 限流必需 + 管理後台系統)
# ⚠️ 請執行: npx wrangler kv:namespace create "FLUX_KV"
# 然後將生成的 id 填入下方
#
# KV 存儲用途:
# ────────────────────────────────────────────
# 【Nano Pro 限流和配額管理】
# - rate_limit:{ip} (IP 限流計數)
# ────────────────────────────────────────────
# 【管理後台系統 - 身份驗證】
# - admin:credentials (管理員憑證)
# - admin:tokens:{token} (JWT Token)
# ────────────────────────────────────────────
# 【管理後台系統 - 風格管理】
# - admin:styles (自定義風格配置)
# ────────────────────────────────────────────
# 【管理後台系統 - 模型配置】
# - admin:providers (供應商配置)
# - admin:custom_models (自定義模型)
# - admin:custom_providers (自定義供應商)
# ────────────────────────────────────────────
# 【管理後台系統 - 參數調整】
# - admin:parameters (參數配置)
# ────────────────────────────────────────────
# 【統計與監控】
# - online:{ip} (在線用戶計數，60s TTL)
# - stats:daily:{date} (每日請求統計)
# ============================================
[[kv_namespaces]]
binding = "FLUX_KV"
id = "65c2232e78044e749adb797599cb6ac8"

# ============================================
# Workers AI 綁定（用於中文自動翻譯）
# ============================================
[ai]
binding = "AI"

# ============================================
# 環境變量（非敏感）
# ============================================
[vars]
WORKER_VERSION = "11.15.0"
DEFAULT_MODEL = "flux-schnell"
MAX_WIDTH = 4096
MAX_HEIGHT = 4096
MAX_TIMEOUT = 300000
ENVIRONMENT = "production"

# ============================================
# Secrets 通過 wrangler secret 命令設置
# 🔐 推薦設置:
# ============================================
# 【API 供應商 Keys】
# 1. POLLINATIONS_API_KEY (Pollinations 圖像生成)
# 2. INFIP_API_KEY (Ghostbot/Infip)
# 3. AQUA_API_KEY (Aqua Server)
# 4. KINAI_API_KEY (Kinai API)
# 5. AIRFORCE_API_KEY (Airforce API)
# 6. NONPON_API_KEY (Nonpon API)
#
# 【自定義供應商 API Keys】
# 可在管理後台「模型配置」頁面直接配置
# 支援 OpenAI 兼容、Stability AI 等自定義供應商
#
# 【管理後台配置】
# 默認管理員帳號: admin
# 默認密碼: admin123
# ⚠️ 首次登入後請立即修改密碼！
# ============================================
# 設置命令範例：
# npx wrangler secret put POLLINATIONS_API_KEY
# npx wrangler secret put INFIP_API_KEY
# npx wrangler secret put AQUA_API_KEY
# npx wrangler secret put KINAI_API_KEY
# npx wrangler secret put AIRFORCE_API_KEY
# npx wrangler secret put NONPON_API_KEY
#
# 查看所有 secrets:
# npx wrangler secret list
#
# 刪除 secret:
# npx wrangler secret delete SECRET_NAME
